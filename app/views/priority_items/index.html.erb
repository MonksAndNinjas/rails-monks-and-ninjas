<%= render partial: "layouts/index", locals: { :list_or_table => "table" } %>
<!-- Have three priority items been created? If so, no more can be created -->
<%= display_add_item_link?(@all_of_nested_attribute) %>
